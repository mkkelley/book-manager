apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "book-manager.fullname" . }}-test-connection"
  labels:
    {{- include "book-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "book-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
