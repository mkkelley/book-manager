apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-manager.fullname" . }}
  labels:
    {{- include "book-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-manager.selectorLabels" . | nindent 4 }}
